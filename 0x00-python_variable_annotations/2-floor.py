#!/usr/bin/env python3
""" Contains `floor` """
from math import floor as F


def floor(n: float) -> int:
    """ Returns the floor of `n`

    Args:
        n (float): _Float number_

    Returns:
        float: _floor of `n`_
    """
    return F(n)
